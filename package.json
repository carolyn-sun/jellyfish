{
  "name": "twitter-agent-worker-monorepo",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "wizard": "pnpm --filter wizard start",
    "dev": "pnpm --filter dashboard run build && pnpm --filter worker run dev",
    "build": "pnpm --filter dashboard run build",
    "deploy": "pnpm --filter dashboard run build && pnpm --filter worker run deploy",
    "typecheck": "pnpm --filter worker run typecheck"
  }
}
